spring:
  application:
    name: notification
  datasource:
    username: mushtaq
    password: ushallpass
    url: jdbc:postgresql://localhost:5432/notifications
  jpa:
    hibernate:
      ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: true
      show-sql: true

server:
  port: 8082